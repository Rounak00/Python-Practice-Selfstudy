# Vector store need for store vctorsand smart first semantic search
# Vector Store and Vector DB
# Store is  a system with retirval and if we have DB features like backup and restore, ACID, Concurrency those are vector DB

# nothing here mostly there some functions same for all vector store or DB in langchain 

# we use different embedding models here to create embeddings